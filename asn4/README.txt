This was made on flip 1
There are no command line arguments and reads from a file called data.txt
It outputs to a file called change.txt and outputs all results from all lines

run with "python makechange.py"